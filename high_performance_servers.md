## 高性能并发服务器开发流程
### 一、架构设计阶段
1. 整体架构选型
   - 分层设计：采用网络层、协议层、业务逻辑层、存储层的垂直分层，层间通过接口解耦
   - 模块化设计：核心模块（网络、线程池、内存管理等）独立封装，支持按需组合
   - 扩展性架构：预留插件接口，支持功能模块热插拔（如新增协议、加密算法）
2. 技术栈选型
   - 编译标准：C++17 及以上（利用std::variant、std::any、constexpr等特性）
   - 需要用到的库 boost库
### 二、核心模块开发
1. 并发线程池（动态可调整）
 - 动态扩缩容：
   - 触发条件：任务队列长度阈值、CPU 利用率、平均任务耗时
   - 调整策略：平滑增减线程（避免抖动），保留最小核心线程数
   - 任务优先级：多级优先级队列，确保关键任务优先执行
2. 网络模块（TCP/UDP 支持）
 - 连接管理：
   - TCP：连接池（长连接复用）、心跳检测（应用层 ping）、半连接队列控制
   - UDP：会话管理（基于源 IP + 端口）、包序重组、丢包重传机制
 - 协议适配层：
   - 统一接口抽象：IProtocol基类，派生TcpProtocol、UdpProtocol
   - 支持协议切换：同一端口可动态切换协议（通过首部标识区分）
3. 内存管理
 - 多级内存池：
   - 线程私有缓存（快速分配）→ 中心内存池（中等分配）→ 系统堆（大内存）
   - 按大小分类的内存块（如 8B/16B/32B/64B/128B/256B/512B/1KB/4KB 等）
 - 智能指针扩展：
   - 基础：std::shared_ptr+std::weak_ptr管理共享资源
   - 扩展：自定义ObjectPoolPtr（结合内存池的对象复用指针）
 - 内存监控：
   - 分配 / 释放统计（按模块、按大小）
   - 内存碎片率监控
4. 回调与事件机制
  - 回调管理：
    - 类型安全的回调注册表：CallbackRegistry<ProtocolType, Args...>
    - 支持同步 / 异步回调，回调执行线程指定（网络线程 / 业务线程）
  - 事件总线：
    - 基于观察者模式的全局事件系统（如连接建立 / 断开、配置变更）
    -  事件订阅 / 发布接口：Subscribe(event_type, callback)、Publish(event)
5. 日志系统
  - 核心特性：
    - 异步写入（前端收集 + 后端线程批量刷盘）
    - 分级日志：TRACE/DEBUG/INFO/WARN/ERROR/FATAL
    -  多输出目标：文件、控制台、远程日志服务器
  - 增强功能：
    - 日志结构化（JSON 格式，便于分析）
    - 上下文信息：线程 ID、模块名、时间戳（精确到微秒）
    - 日志采样（高负载时降低 DEBUG 日志频率）
  - 敏感信息脱敏
    - 内置脱敏规则引擎，自动识别并替换日志中的敏感数据（如手机号、身份证号、密码、Token），支持自定 - 义脱敏策略（如保留前 3 后 4 位、全替换为 *），兼顾日志可用性与数据安全。
    - 脱敏操作在日志生成阶段完成，避免敏感信息落地磁盘。
  - 操作审计日志：
    - 记录所有日志访问行为（如 “用户 A 在 10:00 查询了 db 模块的 ERROR 日志”），确保日志本身的访问可追溯，符合合规要求。
  - 日志队列溢出保护：
    - 当日志产生速度超过处理速度（如突发流量导致日志暴增），前端缓冲区（环形队列）满时，自动触发保护策略：优先保留ERROR/FATAL级日志，丢弃部分DEBUG/TRACE日志，并记录 “日志丢弃数” 指标，避免日志系统阻塞业务线程。
  - 组件故障隔离：
    - 日志后端刷盘线程、远程日志发送线程故障时（如磁盘 IO 挂死、远程服务器宕机），自动切换至 “降级模式”（仅内存缓存关键日志），并通过事件总线触发告警，同时不影响业务主流程。
  - 本地轻量检索：
    - 内置简易查询接口，支持按关键词、时间范围、模块、级别等条件快速筛选日志（如log_query("level:ERROR AND module:db AND time:2024-08-09")），避免依赖外部工具的实时性问题。
  - 智能轮转规则：
    - 支持按时间（如每小时 / 每天）、大小（如单个文件达到 1GB）、日志级别（如ERROR日志单独轮转）触发轮转，避免单一日志文件过大导致的读写性能下降或检索困难。
  - 延迟日志批量合并（异步无锁）
    - 核心思路：对高频重复的日志（如 “连接建立成功”“心跳包接收”），不立即写入，而是在内存中暂存计数（如 “1 秒内收到 1000 次相同日志”），到期后合并为一条日志（“[批量] 连接建立成功 x1000”）。
    性能保障：通过线程本地计数器（TLS）+ 定时合并（毫秒级）实现，全程无锁，仅在合并时产生一次写入，比每条日志单独写入减少 99%+ 的 IO 操作，尤其适合高并发场景。